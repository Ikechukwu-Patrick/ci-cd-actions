spring.application.name=newUser
server.port=8080
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/orders}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:111555pat@gmail}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.docker.compose.lifecycle.readiness.timeout=2m
# Expose the health and info endpoints
management.endpoints.web.exposure.include=health,info
# Show full health details
management.endpoint.health.show-details=always


